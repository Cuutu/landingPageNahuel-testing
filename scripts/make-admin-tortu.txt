═══════════════════════════════════════════════════════════════════════════════
  SCRIPT PARA ASIGNAR ADMIN AL USUARIO DE PRUEBA
═══════════════════════════════════════════════════════════════════════════════

OPCIÓN 1: Si conocés el email exacto del usuario "admin":

───────────────────────────────────────────────────────────────────────────────

const email = "admin@ejemplo.com";  // ← REEMPLAZA con el email real
db.users.updateOne(
  { email: email },
  { $set: { role: "admin" } }
);
print("✅ Usuario " + email + " ahora es administrador");

───────────────────────────────────────────────────────────────────────────────

OPCIÓN 2: Buscar usuarios que contengan "admin" en el email o nombre:

───────────────────────────────────────────────────────────────────────────────

// Buscar usuarios con "admin" en el email
const usersWithAdmin = db.users.find({
  $or: [
    { email: /admin/i },
    { name: /admin/i }
  ]
}).toArray();

print("🔍 Usuarios encontrados con 'admin':");
usersWithAdmin.forEach((user, index) => {
  print((index + 1) + ". " + user.name + " (" + user.email + ") - Rol: " + (user.role || "normal"));
});

// Si encontraste el usuario, actualizalo:
// db.users.updateOne(
//   { email: "EMAIL_DEL_USUARIO_AQUI" },
//   { $set: { role: "admin" } }
// );

───────────────────────────────────────────────────────────────────────────────

OPCIÓN 3: Ver TODOS los usuarios y elegir:

───────────────────────────────────────────────────────────────────────────────

db.users.find({}, { email: 1, name: 1, role: 1 }).pretty()

// Después de ver la lista, actualiza el que necesites:
// db.users.updateOne(
//   { email: "EMAIL_DEL_USUARIO" },
//   { $set: { role: "admin" } }
// );

───────────────────────────────────────────────────────────────────────────────

OPCIÓN 4: Script completo que busca y actualiza automáticamente:

───────────────────────────────────────────────────────────────────────────────

// Buscar usuario que contenga "admin" en email o nombre
const user = db.users.findOne({
  $or: [
    { email: /admin/i },
    { name: /admin/i }
  ]
});

if (user) {
  print("✅ Usuario encontrado:");
  print("   Nombre: " + user.name);
  print("   Email: " + user.email);
  print("   Rol actual: " + (user.role || "normal"));
  
  if (user.role === "admin") {
    print("ℹ️ El usuario ya es administrador");
  } else {
    db.users.updateOne(
      { _id: user._id },
      { $set: { role: "admin", updatedAt: new Date() } }
    );
    print("🎉 ¡Usuario promovido a administrador!");
  }
} else {
  print("❌ No se encontró ningún usuario con 'admin' en email o nombre");
  print("💡 Ejecuta esto para ver todos los usuarios:");
  print("   db.users.find({}, { email: 1, name: 1, role: 1 }).pretty()");
}

───────────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════════
